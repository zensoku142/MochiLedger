# MochiLedger Backend

后端工程目录，计划使用 Java、Spring Boot 和 MySQL。

后续后端源码、数据库迁移、测试和部署配置均放在此目录中，与 `frontend/` 的 React Native 工程独立维护。

## Java 环境

本工程使用 Temurin Java 21，并通过 `.jabbarc` 固定精确版本。在本目录中执行以下命令即可切换：

```powershell
jabba use
```
