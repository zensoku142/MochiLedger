# MochiLedger

MochiLedger 是一个多端记账项目，前后端工程在同一仓库中独立维护。

## 工程目录

- `frontend/mobile/`：React Native 用户端工程。
- `frontend/admin/`：预留的 React Web 管理后台工程。
- `backend/`：Java、Spring Boot、MySQL 后端工程。

各工程的依赖、构建命令和环境配置分别保存在对应目录中，不在仓库根目录混用。
